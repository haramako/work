var PPU_CTRL1 :int options (address:0x2000);
var PPU_CTRL2 :int options (address:0x2001);
var PPU_STAT  :int options (address:0x2002);
var PPU_SCROLL:int options (address:0x2005);
var PPU_ADDR  :int options (address:0x2006);
var PPU_DATA  :int options (address:0x2007);

function ppu_put( addr:int*, size:int ):int
{
  var i:int = 0;
  while( i < size ){
    PPU_DATA = addr[i];
    i += 1;
  }
  return 0;
}

function ppu_fill( size:int, num:int ):int
{
  var i:int = 0;
  while( i < size ){
    PPU_DATA = num;
    i += 1;
  }
  return 0;
}

function ppu_print( str:int* ): int
{
  var i:int = 0;
  while( str[i] ){
    PPU_DATA = str[i];
    i += 1;
  }
  return 0;
}

function ppu_lock():int
{
  PPU_CTRL1 = 0b10000000;
  PPU_CTRL2 = 0b00000000; // 画面表示をOFF
  return 0;
}

function ppu_unlock():int
{
  PPU_SCROLL = 0;
  PPU_SCROLL = 0;
  PPU_CTRL1 = 0b10000000;
  PPU_CTRL2 = 0b00011110; // 画面表示をON
  return 0;
}
