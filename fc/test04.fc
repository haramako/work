// comment
/* comment */
// const CONST:int = 10+10;

include( "nes.fc" );
var vsync_flag:int;

function main(): int
{

  ppu_lock();
  
  PPU_ADDR = 0x3F;
  PPU_ADDR = 0x00;
  PPU_DATA = 0x0F;
  PPU_DATA = 0x11;
  PPU_DATA = 0x12;
  PPU_DATA = 0x13;

  //ppu_fill( 32, 256 );

  PPU_ADDR = 0x20;
  PPU_ADDR = 0x80;
  ppu_print( "HOGE.FUGA" );
  wait_vsync();
  ppu_unlock();

  return 0;
}


function interrupt():int
{
  vsync_flag = 0;
  return 0;
}

function wait_vsync():int
{
  vsync_flag = 1;
  while( vsync_flag ){}
  return 0;
}
